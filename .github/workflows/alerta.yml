name: Notificacion de Despliegue

on:
  push:
    branches:
      - main

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Enviar alerta a ntfy.sh
        run: |
          curl -d "Despliegue exitoso por parte de Alan Garcia(2024-0040)" ntfy.sh/devops-itla
